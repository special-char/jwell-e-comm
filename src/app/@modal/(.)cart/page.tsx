import SidePanel from "@/components/common/sidePanel";

type Props = {};

const Cart = (props: Props) => {
  return (
    <SidePanel>
      <div>Cart</div>
    </SidePanel>
  );
};

export default Cart;
