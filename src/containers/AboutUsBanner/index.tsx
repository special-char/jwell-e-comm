import React from "react";
import Banner from "@/components/common/Banner";
import { aboutUsPageBanner } from "@/lib/data";

const AboutUsBanner = () => {
	return <Banner data={aboutUsPageBanner} />;
};

export default AboutUsBanner;
